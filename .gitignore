# Data Dragon / patch caches (re-downloaded on demand)
data/cache/
# Python
__pycache__/
*.pyc
.venv/
# Rust / Tauri (M1+)
target/
# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# raw --dump captures: contain chat tokens and ids; scrub into named fixtures instead
m0/tests/fixtures/captured/

# Tauri overlay build output (built on the Windows side, see scripts/cargo-win.sh)
overlay/target/
overlay/src-tauri/gen/

# local screenshots for overlay checks
.screens/
